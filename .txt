const { exec } = require('child_process');
exec('/Users/theodornecula/Desktop/GitHubRepo/Jimmy_AI/discord_venv/Scripts/python.exe -I ...', (error, stdout, stderr) => {
    if (error) {
        console.error(`exec error: ${error}`);
        return;
    }
    console.log(`stdout: ${stdout}`);
    console.error(`stderr: ${stderr}`);
});
exec('C:\\Users\\theodornecula\\Desktop\\GitHubRepo\\Jimmy_AI\\discord_venv\\Scripts\\python.exe -I ...', (error, stdout, stderr) => {
    // handle error and output
});
